# 分布式组件
插件打包后是一个jar包，通过把jar包上传到服务器指定目录下来安装这个jar包，在单机环境下通过安装卸载接口能够让jar包即使生效和失效。<br/>
在微服务场景下，多个微服务启动时，可以从指定目录加载插件jar包，让插件生效。当调用卸载接口时，负载均衡器会将请求分发到其中一个微服务，该微服务卸载插件，其他微服务并不会卸载。<br/>
这个组件就是用来解决该问题的。

## 原理

![img.png](../images/plugin-distributed-img.png)

